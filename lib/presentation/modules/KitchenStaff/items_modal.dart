
import 'package:flutter/material.dart';

class ItemsModal extends StatelessWidget {
  const ItemsModal({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}